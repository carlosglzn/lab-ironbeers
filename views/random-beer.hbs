{{#each responseFromAPI}}

     <div>

        <img src="{{image_url}}" alt="Beer">
        <h1>{{name}}</h1>
        <p>{{description}}</p>
        <p>{{tagline}}</p>
        <p>{{food_pairing}}</p>
        <p>{{brewers_tips}}</p>

    </div>

{{/each}}